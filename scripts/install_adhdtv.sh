#!/bin/bash

# AD-HDTV installer alias.
set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# No legacy install_webgridplayer.sh call. All install logic should be here or in AD-HDTV scripts only.
