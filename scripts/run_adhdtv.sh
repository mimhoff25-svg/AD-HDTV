#!/bin/bash

# AD-HDTV launcher alias.
set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
# No legacy run_webgridplayer.sh call. All run logic should be here or in AD-HDTV scripts only.
